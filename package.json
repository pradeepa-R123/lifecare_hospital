{
  "name": "lifecare-hospital",
  "version": "1.0.0",
  "description": "HealthCare Hospital Management System with AI Chatbot",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd frontend && npm install && npm run build && cp -r dist/* ../public/",
    "install:all": "npm install && cd frontend && npm install",
    "setup": "npm run install:all && npm run build"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "groq-sdk": "^0.3.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.0",
    "react-router-dom": "^7.13.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
